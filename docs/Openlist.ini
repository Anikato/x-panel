Openlist

location / {
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;
  proxy_set_header Host $http_host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header Range $http_range;
  proxy_set_header If-Range $http_if_range;
  proxy_redirect off;
  proxy_pass http://127.0.0.1:5244;
  proxy_http_version 1.1;
  # the max size of file to upload
  client_max_body_size 20000m;
}

Minio

    location / {
        # **关键：代理到 MinIO Console 端口 9001**
        proxy_pass http://127.0.0.1:9001/;
        
        # 头部设置
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        # MinIO Console 需要 WebSocket 连接
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 优化上传/下载性能的配置
        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
        proxy_buffering off;
        proxy_request_buffering off;
        client_max_body_size 0; # 允许上传任意大小文件
        proxy_redirect off;
    }

nl5255.933000.xyz - node 

    location /gr-n-5 {
        if ($content_type !~ "application/grpc" ) {
            return 401; 
        }
        client_max_body_size 0; 
        client_body_buffer_size 1m; 
        client_body_timeout 1h; 
        grpc_pass grpc://127.0.0.1:22116; 
        grpc_set_header X-Real-IP $remote_addr; 
    }
    location /nlny {
        if ($content_type !~ "application/grpc" ) {
            return 401; 
        }
        client_max_body_size 0; 
        client_body_buffer_size 1m; 
        client_body_timeout 1h; 
        #grpc_pass grpc://10.1.0.201:16001;
        grpc_pass grpc://107.174.34.124:16002; 
        grpc_set_header X-Real-IP $remote_addr; 
    }

immich-app

    location / {
        proxy_pass http://127.0.0.1:2283; 
    }